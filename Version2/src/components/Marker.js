import React from 'react'

const Marker = (props) => {
    return (
        <div>
                <h4>{props.title}</h4>
              <p>{props.context}</p>
            
        </div>
    )
}

export default Marker
